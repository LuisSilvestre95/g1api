<?php

// autoload.php @generated by Composer

if (PHP_VERSION_ID < 50600) {
    handleOldPhpVersion();
}

require_once __DIR__ . '/composer/autoload_real.php';

return ComposerAutoloaderInit2f6ec873b5f0716b5235eed5b27e71e5::getLoader();

/**
 * Handles the case where the PHP version is less than 5.6.
 */
function handleOldPhpVersion() {
    if (!headersSent()) {
        sendInternalServerErrorHeader();
    }
    $err = generateErrorMessage();
    displayError($err);
    trigger_error($err, E_USER_ERROR);
}

/**
 * Checks if headers have already been sent.
 *
 * @return bool
 */
function headersSent() {
    return headers_sent();
}

/**
 * Sends a 500 Internal Server Error header.
 */
function sendInternalServerErrorHeader() {
    header('HTTP/1.1 500 Internal Server Error');
}

/**
 * Generates the error message for old PHP versions.
 *
 * @return string
 */
function generateErrorMessage() {
    return 'Composer 2.3.0 dropped support for autoloading on PHP <5.6 and you are running ' . PHP_VERSION . ', please upgrade PHP or use Composer 2.2 LTS via "composer self-update --2.2". Aborting.' . PHP_EOL;
}

/**
 * Displays the error message based on the SAPI.
 *
 * @param string $err
 */
function displayError($err) {
    if (!ini_get('display_errors')) {
        if (PHP_SAPI === 'cli' || PHP_SAPI === 'phpdbg') {
            fwrite(STDERR, $err);
        } elseif (!headersSent()) {
            echo $err;
        }
    }
}